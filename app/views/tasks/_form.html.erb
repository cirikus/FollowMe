<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :openingDate %><br>
    <%= f.date_select :openingDate %>
  </div>
  <div class="field">
    <%= f.label :closingDate %><br>
    <%= f.date_select :closingDate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
